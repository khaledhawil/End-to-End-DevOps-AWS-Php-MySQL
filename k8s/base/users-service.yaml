apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: tms-app
spec:
  selector:
    app: users-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
