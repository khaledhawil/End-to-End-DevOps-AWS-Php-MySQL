apiVersion: v1
kind: Service
metadata:
  name: logout-service
  namespace: tms-app
spec:
  selector:
    app: logout-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
