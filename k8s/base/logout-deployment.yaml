apiVersion: apps/v1
kind: Deployment
metadata:
  name: logout-service
  namespace: tms-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: logout-service
  template:
    metadata:
      labels:
        app: logout-service
    spec:
      containers:
        - name: logout-service
          image: khaledhawil/task-manager-task:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: sql-endpoint
                  key: endpoint
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: db-username
                  key: username
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: db-password
                  key: password
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-password
                  key: password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: db-name
                  key: name
            - name: DB_PORT
              value: "3306"
